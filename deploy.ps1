# Uncomment after updating with correct path and variables
#Set-Location "C:\...\fintrack" <- Update with fintrack folder path
#scp -r . <user>@192.168.x.x:/home/<user>/fintrack
#ssh <user>@192.168.x.x "cd /home/<user>/fintrack && sudo docker compose up -d --build"